// Assignment 7: Portfolio Website
// =================================
//
// Build a professional portfolio using Next.js specific optimizations.
//
// Setup: npx create-next-app@latest portfolio-next
//
// Requirements:
// 1. Pages: Home, Projects, About, Contact.
// 2. Optimization 1: Use `next/image` for ALL images (your headshot, project screenshots). Note how it prevents layout shift.
// 3. Optimization 2: Use `next/font` (e.g., Inter or Roboto) for all typography in your `layout.jsx`.
// 4. Optimization 3: Add `metadata` exports to EVERY page (Title and Description) for SEO.
// 5. Build a custom 404 page (`not-found.jsx`).
//
// Concepts Tested:
// - next/image implementation
// - next/font implementation
// - Static metadata generation
// - Custom error pages
//
// Hints:
// - Place static assets like your profile picture in the `public/` directory and reference them starting with `/` (e.g., `src="/profile.jpg"`).
// - For `next/font`, import it at the top of your layout and apply the `.className` to the `<body>` tag.
//
// Bonus:
// - Implement a Dark Mode toggle (Requires a Client Component context provider wrapping your layout).
// - Add animations using Framer Motion (Client Components).
